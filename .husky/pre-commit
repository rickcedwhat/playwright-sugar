#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🚧 Running build and quality checks..."

# Check for changelog updates if version bumped
node scripts/check-version-bump.mjs || { echo "Changelog check failed, commit aborted."; exit 1; }

npm run build || { echo "Build failed, commit aborted."; exit 1; }

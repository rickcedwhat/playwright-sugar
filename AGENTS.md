# Agent notes

## Pull request authorship

All agent-opened PRs **must** be authored by [`rickcedwhat-ai`](https://github.com/rickcedwhat-ai), not `rickcedwhat`.

See [`.github/MAINTENANCE.md`](./.github/MAINTENANCE.md) for the full rule and the `GITHUB_AI_TOKEN` / `gh pr create` procedure. If a PR was opened under the wrong account, close it and re-open the same branch as `rickcedwhat-ai` (same pattern as #64 → #65).
